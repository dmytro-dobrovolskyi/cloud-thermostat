spring:
  cache:
    cache-names: config
  task:
    scheduling:
      pool:
        size: 3
application:
  thermostat:
    delayInMilliseconds: 60000 # 1 min
  readings-publisher:
    delayInMilliseconds: 300000 # 5 min
server:
  servlet:
    context-path: /cloud-thermostat/api
thirdparty:
  brewers-friend:
    api-key: # Do not expose publicly
